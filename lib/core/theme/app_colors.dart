import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Backgrounds
  static const Color background = Color(0xFF0D1117);
  static const Color surface = Color(0xFF1A2332);
  static const Color surfaceVariant = Color(0xFF243040);

  // Neon accents
  static const Color primaryNeon = Color(0xFF00F5FF); // cyan
  static const Color secondaryNeon = Color(0xFF39FF14); // green
  static const Color accentPurple = Color(0xFFBF5FFF); // purple
  static const Color errorRed = Color(0xFFFF4444); // red

  // Text
  static const Color textPrimary = Color(0xFFE8F4F8);
  static const Color textSecondary = Color(0xFF8BA3B0);
  static const Color textDisabled = Color(0xFF4A5568);

  // Borders / dividers
  static const Color border = Color(0xFF2D3F54);
  static const Color divider = Color(0xFF1E2D3D);

  // Transparent overlays
  static const Color overlayLight = Color(0x1AFFFFFF);
  static const Color overlayDark = Color(0x80000000);
}
